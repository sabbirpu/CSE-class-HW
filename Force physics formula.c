#include <stdio.h>

int main() {
    // Write C code here
   float m,a,F;
    printf("Enter the value of m(mass):");
    scanf("%f",&m);
    printf("Enter the value of a(Acceleration):");
    scanf("%f",&a);
    F=m*a;
    printf("Force value=%.2f",F);
}
    
